export interface Customisation {
    highlightKeywords: boolean;   // default false
    questions: boolean;            // default false
    summaryFormat: string;        // default ""
    length: number;                // default 1
}